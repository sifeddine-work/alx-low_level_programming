#include <stdio.h>
/**
 *main - A c program that print a Text with printf function
 *Return: Always 0 (Success)
*/
int main(void)
{
puts("with proper grammar, but the outcome is a piece of art,");
return (0);
}
